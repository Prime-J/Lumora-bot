Drop scroll images here. The bot looks for files named <scroll_id>.{png,jpg,jpeg,webp}.

Current scroll IDs (from data/scrolls.json):
  windworn_scroll
  dawn_etched_scroll
  tide_sealed_scroll
  iron_seal_scroll
  void_etched_scroll
  ember_tongue_scroll
  shieldbearers_scroll
  caligo_vellum
  starlit_codex
  broken_communion_scroll
  lumen_folio
  first_keepers_scroll

If a file with one of those names exists, the bot will include it as the
image when a player runs .open <scroll>. If not, the bot falls back to
text-only — no error.
